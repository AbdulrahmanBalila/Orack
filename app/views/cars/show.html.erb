<p style="color: green"><%= notice %></p>

<%= render @car %>

<div>
  <%= link_to "Edit this car", edit_car_path(@car) %> |
  <%= link_to "Back to cars", cars_path %>
  <br/>
  <%= button_to "Delete", car_path(@car), data: { turbo_method: :delete, turbo_confirm: "U Sure?" } %></td>
</div>
